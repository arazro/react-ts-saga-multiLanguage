import React from "react";
import { render } from "@testing-library/react";


describe("<home />", () => {
  test("should display a blank login form, with remember me checked by default", async () => {

    render(<h1>Hello, Shetac!</h1>)
  });

});

describe("<home />", () => {
  test("should display a blank login form, with remember me checked by default", async () => {

    render(<h1>Hello, Shetac!</h1>)
  });

});